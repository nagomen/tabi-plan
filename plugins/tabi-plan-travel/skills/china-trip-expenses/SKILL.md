---
name: china-trip-expenses
description: 香港・マカオ・金門旅行の旅程や費用を確認し、利用者の確認後に旅程・費用を追加・訂正・並び替え・削除・復元する。
---

# 香港・マカオ・金門旅行

このスキルは、この旅行だけに接続された Tabi Plan MCP を使う。

## 旅程の扱い

1. まず `get_itinerary` または `search_itinerary` を呼び、対象の `itemId` と最新の `version` を確認する。
2. 追加・訂正・移動・削除の直前に、変更前後を短い日本語で示して確認を得る。日時、場所、交通手段、所要時間を推測しない。
3. 書き込みには直前に取得した `version` を `expectedVersion` として指定する。競合した場合は再取得し、利用者の確認なしに変更を作り直さない。
4. 追加には毎回新しいUUIDを `requestId` として指定し、同じ追加の再試行だけ同じUUIDを使う。
5. 日付や並び順の変更には `move_itinerary_item` を使う。削除前には対象を明示し、復元時は `list_deleted_itinerary_items` で対象を確認する。

## 費用の扱い

1. まず `list_trip_expenses` を呼び、旅行内メンバーID、基準通貨、既存費用を確認する。
2. 内容、支払日、支払額、通貨、換算レート、支払者、負担者と負担額、支払方法を推測しない。不足があれば質問する。
3. 追加・訂正・削除の直前に、変更後の内容を短い日本語で利用者へ示し、確認を得る。
4. 外貨の換算レートは支払日ごとの確定値として扱う。既存費用のレートを現在値で上書きしない。
5. 複数件は、先に全件を編集可能な一覧として提示してから、一件ずつ保存結果を確認する。
6. 削除には `delete_trip_expense` を使う。物理削除ではなく、必要なら `restore_trip_expense` で戻す。

旅行IDや別旅行を入力として組み立てない。このプラグインの対象は香港・マカオ・金門旅行だけである。
