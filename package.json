{
  "name": "tabi-plan",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "contracts",
    "frontend",
    "api"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "dev:full": "node tools/dev-full.mjs",
    "build": "npm run build -w frontend",
    "test": "npm test -w frontend && npm test -w api",
    "test:coverage": "npm run test:coverage -w api",
    "ci": "npm run typecheck && npm test && npm run build",
    "preview": "npm run preview -w frontend",
    "typecheck": "npm run typecheck -w frontend && npm run typecheck -w api",
    "build:api": "npm run build -w api"
  }
}
